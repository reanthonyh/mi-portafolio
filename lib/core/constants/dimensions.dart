import 'package:flutter/widgets.dart';

const smallRadius = Radius.circular(8);
const mediumRadius = Radius.circular(14);
const largeRadius = Radius.circular(20);

const Size smallNavBarSize = Size.fromWidth(240);
const Size normalNavBarSize = Size.fromWidth(240);
const Size largeNavBarSize = Size.fromWidth(240);

const smallPadding = EdgeInsets.all(16);
const largePadding = EdgeInsets.all(32);
const cardPadding = EdgeInsets.symmetric(vertical: 13, horizontal: 16);
